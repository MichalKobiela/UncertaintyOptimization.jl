var documenterSearchIndex = {"docs":
[{"category":"section","location":"yaml/#YAML-Model-Files","page":"YAML Models","text":"YAML files define the mechanistic model and parameter metadata used by the full workflow. They do not define the full run: observation times, initial conditions, solver choice, observed states, observed data, loss functions, and sampling settings are supplied later in Julia with SimulationSpec, TuringSpec, and CartesianScanner.\n\nThe most important YAML choice is each parameter's role. uncertain parameters are inferred from observed data, design parameters are controllable variables optimized during Thompson sampling, and fixed parameters are held constant or used to represent staged experimental conditions.\n\nA minimal example file is included in the repository at docs/src/examples/minimal_model.yml.\n\nexperiment:\n  name: \"MinimalDoseResponse\"\n  description: \"One-state example showing fixed, uncertain, design, warmup, and staged parameters.\"\n\nmodel:\n  type: \"ODE\"\n  states: [\"X\"]\n\nequations:\n  X: \"drive * dose - decay * X + baseline\"\n\nparameters:\n  drive:\n    value: 1.0\n    role: design\n    bounds: [0.1, 5.0]\n    design:\n      warmup_value: 0.5\n      value: 1.5\n\n  decay:\n    value: 0.25\n    role: uncertain\n    prior:\n      distribution: uniform\n      lower: 0.05\n      upper: 1.0\n\n  dose:\n    warmup_value: 0.0\n    value: [0.5, 1.0, 2.0]\n    role: fixed\n\n  baseline:\n    value: 0.1\n    role: fixed\n\n\nLoad and compile it in the same way as any other model:\n\nusing ModelingToolkit: @mtkcompile, System, t_nounits as t\nusing UncertaintyOptimization\n\nmodel_def = load_model(\"docs/src/examples/minimal_model.yml\")\n@mtkcompile sys = System(model_def.equations, t)\nmodel = Model(model_def, sys)","title":"YAML Model Files"},{"category":"section","location":"yaml/#Top-Level-Sections","page":"YAML Models","text":"experiment stores human-readable provenance. name and description are copied into ModelDefinition.\n\nmodel declares the model type and state names. ODE models are the active path today. Every state listed here becomes a time-dependent ModelingToolkit variable.\n\nequations maps each state name to the right-hand side of its differential equation. Equations are Julia expressions written as strings and may refer to states and parameters.\n\nparameters defines every parameter used by the equations. Each parameter has a role, and may also define values, priors, bounds, warmup values, and design-stage metadata. This is where uncertain parameters and design parameters enter the workflow.","title":"Top-Level Sections"},{"category":"section","location":"yaml/#Parameter-Roles","page":"YAML Models","text":"fixed parameters are ordinary scalar or staged values. They are not sampled during inference and are not optimized as design parameters. Use them for parameters that can be treated as known with high confidence, or for staged experimental conditions. In the example, baseline is scalar and dose is staged.\n\nuncertain parameters are the model parameters whose values are not known before seeing the observed data. They need an initial scalar value and a prior when used with TuringSpec. During inference, run_inference updates those priors into posterior samples. In the example, decay is sampled from a uniform prior.\n\ndesign parameters are controllable parameters that can be changed during design optimization. They can have a normal value, optional bounds, and optional nested design values. In the example, drive is a design parameter that can be changed during Thompson sampling or risk-averse evaluation.","title":"Parameter Roles"},{"category":"section","location":"yaml/#Parameter-Fields","page":"YAML Models","text":"value is the ordinary value used for simulation and inference. A scalar value is used for every solve. A vector value such as [0.5, 1.0, 2.0] is converted to a tuple and creates staged production solves.\n\nwarmup_value is used for a warmup solve before the production solve. After warmup, the final warmup state becomes the initial condition for each production solve.\n\nprior describes the prior distribution for an uncertain parameter. The currently supported prior metadata is:\n\nprior:\n  distribution: uniform\n  lower: 0.05\n  upper: 1.0\n\nbounds records a design range. Bounds are metadata for design workflows; the scanner receives its concrete candidate grid through CartesianScanner.\n\ndesign gives design-stage values for run_scan, which calls simulate! with design = true. design.warmup_value is used during the design warmup solve, and design.value is used for design-stage production solves.\n\nScan candidate ranges are not stored in YAML. Put them in the values field of CartesianScanner(scan = [...]) so the mechanistic model stays separate from the design experiment:\n\nscan = CartesianScanner(\n    simulation = simulation,\n    scan = [\n        (symbol = :drive, values = 0.5:0.25:2.0, kind = :scale),\n    ],\n    loss = loss,\n)","title":"Parameter Fields"},{"category":"section","location":"yaml/#Warmup-Solves","page":"YAML Models","text":"Warmup is useful when an experiment has a pre-stimulus or equilibration stage. Any parameter with warmup_value is set to that value during warmup. Then simulate! replaces the production initial state with the final warmup state.\n\nIn the minimal example, dose is 0.0 during warmup and then takes production values [0.5, 1.0, 2.0].\n\ndose:\n  warmup_value: 0.0\n  value: [0.5, 1.0, 2.0]\n  role: fixed\n\nTo inspect the warmup solution, call:\n\nsim = simulate!(\n    model,\n    simulation;\n    return_simulate = true,\n)\n\nsim.warmup_sol\nsim.sols\n\nsim.warmup_sol is nothing when no warmup values are configured.","title":"Warmup Solves"},{"category":"section","location":"yaml/#Observed-Data-And-Staged-Parameters","page":"YAML Models","text":"Vector-valued parameter values represent multiple production conditions. The model is built once, then simulate! runs one production solve per vector position.\n\ndose:\n  warmup_value: 0.0\n  value: [0.5, 1.0, 2.0]\n  role: fixed\n\nThis creates three production solves, which correspond to three observed trajectories in the observed data:\n\ndose = 0.5\ndose = 1.0\ndose = 2.0\n\nIf more than one parameter has a vector value, their vector lengths must match. Values are paired by position. Scalar parameters are reused for every staged solve.\n\nDuring inference, each posterior proposal is evaluated against all staged production solves. The observed data is flattened internally and must match:\n\nlength(simulation.t_obs) *\nobserved_state_count(simulation) *\nnumber_of_staged_production_solves\n\nFor the minimal example, one observed state, ten observation times, and three staged dose values require 30 observations. The order is stage first, then observed state, then time. For one state this means all time points for dose = 0.5, then all time points for dose = 1.0, then all time points for dose = 2.0. This flattened vector represents the observed trajectories.","title":"Observed Data And Staged Parameters"},{"category":"section","location":"yaml/#Design-Stage-Values","page":"YAML Models","text":"Design-stage scans use the same model but may need different values from the ordinary inference stage. Put those under a nested design block.\n\ndrive:\n  value: 1.0\n  role: design\n  bounds: [0.1, 5.0]\n  design:\n    warmup_value: 0.5\n    value: 1.5\n\nWhen run_scan evaluates a candidate, the default evaluator calls simulate! with design = true. This means design warmup values and design production values are used where they are defined, while uncertain parameters still come from the posterior sample. This lets the same mechanistic model support both posterior inference and risk-averse design.","title":"Design Stage Values"},{"category":"section","location":"workflow/#Workflow","page":"Workflow","text":"UncertaintyOptimization supports risk-averse optimization under uncertainty. A study is organized around mechanistic model definition, Bayesian inference for uncertain parameters, Thompson-sampling-based design optimization, and risk-averse selection of final designs.\n\nThe main stages are:\n\nModel the system.\nDefine the design goal.\nIdentify uncertain parameters and design parameters.\nInfer uncertain parameters from observed data.\nOptimize designs via Thompson sampling.\nManage risk and select final design(s).\n\nA reusable simulation/build layer sits around those stages. The same loaded model and SimulationSpec can be reused so expensive ModelingToolkit and SciML setup is paid once and then reused by inference, Thompson sampling, and evaluation.","title":"Workflow"},{"category":"section","location":"workflow/#Model-The-System","page":"Workflow","text":"The YAML file describes the symbolic model, not the full experiment execution. load_model reads this file and returns a ModelDefinition. See YAML Model Files for a complete minimal file and details about parameter roles, warmup values, and staged parameters.\n\nRequired model sections are:\n\nexperiment: a name and description for provenance.\nmodel: model type and state names.\nparameters: parameter values, roles, priors, and optional design metadata.\nequations: one right-hand-side expression for each declared state.\n\nParameter roles encode how each parameter is used:\n\nfixed: a high-confidence constant or staged experimental condition.\nuncertain: a parameter inferred from observed data through its prior and posterior distribution.\ndesign: a controllable parameter that may be changed during design optimization and risk-averse evaluation.\n\nusing ModelingToolkit: @mtkcompile, System, t_nounits as t\nusing UncertaintyOptimization\n\nmodel_def = load_model(\"model.yml\")\n@mtkcompile sys = System(model_def.equations, t)\nmodel = Model(model_def, sys)\n\nSimulation settings such as observation times, observed states, solver choice, initial conditions, and time span are supplied with SimulationSpec in Julia code. This keeps the mechanistic model separate from the observed-data and design-optimization stages.","title":"Model The System"},{"category":"section","location":"workflow/#Compile-And-Simulate","page":"Workflow","text":"SimulationSpec describes how a model should be solved for inference, Thompson sampling, risk-averse evaluation, or direct simulation. simulate! is the main simulation entry point. On its first call it delegates to setup_simulation!, which builds and caches the ODEProblem and the parameter setters used by later calls.\n\nusing OrdinaryDiffEq: Rosenbrock23\n\nsimulation = SimulationSpec(\n    t_obs = collect(range(1.0, 90.0, length = 30)),\n    obs_state = :A,\n    initial_conditions = (1.0, 1.0),\n    tspan = (0.0, 100.0),\n    solver = Rosenbrock23(),\n    solver_opts = (dtmin = 1e-9,),\n)\n\nsols = simulate!(model, simulation)\n\nThese simulated trajectories are the model predictions compared with observed data during inference and with target behavior during design evaluation.","title":"Compile And Simulate"},{"category":"section","location":"workflow/#Infer-Uncertain-Parameters-From-Observed-Data","page":"Workflow","text":"Inference turns prior knowledge and observed data into a posterior distribution over uncertain parameters. Parameters marked uncertain in YAML receive priors, and run_inference samples their posterior with Turing.\n\nFor Turing-based Bayesian inference, create a TuringSpec from a SimulationSpec and observed data, then call run_inference.\n\nusing Distributions: InverseGamma\nusing Turing: NUTS\n\nturing_spec = TuringSpec(\n    simulation = simulation,\n    data = observed_data,\n    noise_prior = InverseGamma(2, 3),\n    noise_initial = 3.0,\n    sampler = NUTS(0.65),\n    n_samples = 3000,\n    n_chains = 1,\n)\n\nchain = run_inference(model, turing_spec)\n\nrun_inference prepares the model for simulation, builds priors for parameters marked uncertain in YAML, calls simulate! inside the Turing model, and returns a chain with parameter names restored to the YAML symbols.\n\nEach row of the returned chain is one posterior sample of the uncertain parameters. Those posterior samples feed directly into the Thompson-sampling stage.","title":"Infer Uncertain Parameters From Observed Data"},{"category":"section","location":"workflow/#Define-The-Design-Goal","page":"Workflow","text":"Desired behavior is encoded as a loss function: low loss means the simulated circuit is close to the target set point, trajectory, amplitude, frequency, or other design objective. The loss function supplied to CartesianScanner plays this role.\n\nfunction loss(warmup_sol, predicted_sol; sys = nothing)\n    target = 50.0\n    prediction = Array(predicted_sol)[end]\n    return (prediction - target)^2\nend\n\nThe default scanner evaluator passes the warmup solution and production solution to this loss. If the design goal needs a different summary, provide a custom loss, or provide a custom evaluator to run_scan.","title":"Define The Design Goal"},{"category":"section","location":"workflow/#Optimize-Designs-Via-Thompson-Sampling","page":"Workflow","text":"Thompson samples are obtained by drawing uncertain parameters from the posterior and optimizing the design parameters for each draw. CartesianScanner implements that idea with a grid of candidate design values. For each posterior sample, run_scan simulates every candidate and marks the lowest-loss candidate as the best design for that posterior draw.\n\nscan = CartesianScanner(\n    simulation = simulation,\n    scan = [\n        (symbol = :kx2, values = LinRange(0.01, 3.0, 100), kind = :scale),\n    ],\n    loss = loss,\n)\n\nthompson_results = run_scan(chain, scan, model)\nchosen_designs = thompson_results[thompson_results.is_best, :]\n\nrun_scan returns a DataFrame. Each row is one posterior sample and one candidate design combination.\n\nFor kind = :scale, each grid value multiplies the posterior draw for an uncertain parameter, or the scalar YAML value for a fixed/design parameter. For kind = :value, the grid value is used directly.\n\nThe rows with is_best == true are the grid-based Thompson samples: one selected design for each posterior draw.","title":"Optimize Designs Via Thompson Sampling"},{"category":"section","location":"workflow/#Manage-Risk-And-Select-Final-Designs","page":"Workflow","text":"Designs are evaluated through the predictive loss distribution under posterior uncertainty and biomolecular noise, then ranked with risk-averse summaries such as upper quantiles to avoid designs that only work in favorable conditions.\n\nEvaluation reuses run_scan, usually over a smaller set of candidate designs chosen during Thompson sampling. The returned DataFrame has one row per posterior sample and candidate, including candidate values, resolved parameter values, loss, is_best, and best_loss.\n\ncandidate_values = unique(chosen_designs.kx2_scaler)\n\nevaluation_scan = CartesianScanner(\n    simulation = simulation,\n    scan = [\n        (symbol = :kx2, values = candidate_values, kind = :scale),\n    ],\n    loss = loss,\n)\n\nevaluation_results = run_scan(chain, evaluation_scan, model)\n\nCommon evaluation summaries group by the candidate columns and aggregate losses, for example median loss, upper quantiles such as the 75% quantile, standard deviation, or the number of times a candidate was selected during Thompson sampling. These summaries provide risk-neutral and risk-averse views of design performance.","title":"Manage Risk And Select Final Designs"},{"category":"section","location":"workflow/#From-Outputs-To-Design-Summaries","page":"Workflow","text":"The returned objects map directly onto the workflow quantities:\n\nchain: posterior samples of uncertain parameters inferred from observed data.\nchosen_designs: Thompson samples, i.e. best candidate designs under individual posterior samples.\nevaluation_results.loss: samples from each candidate design's predictive loss distribution.\ngrouped summaries of evaluation_results: risk-neutral and risk-averse design rankings.\n\nFor example, the median loss approximates typical predictive performance, while an upper quantile such as the 75% quantile is a risk-averse summary:\n\nusing DataFrames: combine, groupby\nusing Statistics: median, quantile\n\nrisk_summary = combine(\n    groupby(evaluation_results, :kx2_scaler),\n    :loss => median => :median_loss,\n    :loss => (x -> quantile(x, 0.75)) => :q75_loss,\n)\n\nCandidates with low median loss and low upper-quantile loss are robust designs: they are predicted to work well on average and to avoid large losses under unfavorable posterior draws.","title":"From Outputs To Design Summaries"},{"category":"section","location":"workflow/#Deeper-Dive","page":"Workflow","text":"","title":"Deeper Dive"},{"category":"section","location":"workflow/#Warmup-And-Production-Time-Spans","page":"Workflow","text":"The warmup stage is activated by specifying a warmup_value for a parameter in the YAML model. Parameters can have separate values for the warmup and production stages: use warmup_value for the warmup solve and value for the production solve. This applies to as many parameters as need staged values.\n\nWhen warmup and production need different time spans, pass two intervals to SimulationSpec.tspan. The first interval is used for the warmup solve, and the second interval is used for each production solve.\n\nsimulation = SimulationSpec(\n    t_obs = collect(range(0.0, 5.45, length = 30)),\n    obs_state = :A,\n    initial_conditions = (1.0, 1.0),\n    tspan = ((0.0, 10.0), (0.0, 5.45)),\n    solver = Rosenbrock23(),\n)\n\nIf only one interval is provided, such as tspan = (0.0, 100.0), it is expanded to ((0.0, 100.0), (0.0, 100.0)) and used for both warmup and production.\n\nIf you want to inspect the warmup trajectory, pass return_simulate = true to simulate!. In that case, simulate! returns (warmup_sol, sols), where warmup_sol is nothing when no warmup values are configured.","title":"Warmup And Production Time Spans"},{"category":"section","location":"workflow/#API-Naming-Notes","page":"Workflow","text":"The workflow examples use CartesianScanner as the public name for grid-based candidate evaluation. CartesianScanner is an alias for ThompsonGridSpec, which is the underlying specification type used by run_scan.","title":"API Naming Notes"},{"category":"section","location":"sampling/#Sampling","page":"Sampling","text":"UncertaintyOptimization uses Markov chain Monte Carlo (MCMC) to infer a posterior distribution for the parameters marked uncertain in the YAML model. The default sampler is Turing's No-U-Turn Sampler (NUTS), an adaptive form of Hamiltonian Monte Carlo (HMC). NUTS is particularly efficient for continuous, differentiable models because it uses posterior gradients to move through many correlated parameters without the slow random walk produced by simple proposal-based samplers.\n\nEfficient sampling is not controlled by one setting. It depends on the posterior geometry, the NUTS trajectory settings, initial parameter values, the numerical ODE solve, and the smoothness of the model equations. This page explains those parts and gives a practical order in which to improve them.","title":"Sampling"},{"category":"section","location":"sampling/#How-NUTS-Explores-A-Posterior","page":"Sampling","text":"Let q contain the uncertain parameters and let p be an auxiliary momentum. HMC defines a Hamiltonian\n\nH(qp) = U(q) + K(p)\n       = -log pi(q) + frac12p^mathsfTM^-1p\n\nwhere pi(q) is the posterior density and M is the mass matrix, also called the metric. A leapfrog integrator follows the Hamiltonian dynamics using the gradient nabla_qlogpi(q). A final Metropolis correction removes the small bias introduced by numerical integration.\n\nNUTS makes HMC easier to use by constructing a binary tree of forward and backward leapfrog steps. It stops extending the trajectory when it detects a U-turn, rather than requiring the user to choose a fixed trajectory length. This lets NUTS take long moves through broad posterior regions and short moves where the trajectory turns quickly.\n\ntip: Intuition: a ball on the posterior landscape\nImagine a ball whose position is the vector of uncertain parameters. The posterior forms a landscape: high-probability parameter combinations are valleys in the corresponding negative log-posterior. The ball needs the partial derivative with respect to every parameter to know the local slope, but it also has its own velocity, represented by the momentum p. The slope bends the trajectory while the momentum carries the ball across the valley. This combination allows HMC to travel much farther than a sampler that repeatedly takes small, directionless steps.","title":"How NUTS Explores A Posterior"},{"category":"section","location":"sampling/#The-Main-NUTS-Controls","page":"Sampling","text":"TuringSpec accepts a configured Turing sampler through its sampler field. For example:\n\nusing AdvancedHMC: DenseEuclideanMetric\nusing Turing: NUTS\n\nsampler = NUTS(\n    1000,\n    0.8;\n    max_depth = 9,\n    init_ϵ = 0.0,\n    metricT = DenseEuclideanMetric,\n)\n\nturing_spec = TuringSpec(\n    simulation = simulation,\n    data = observed_data,\n    noise_initial = 3.0,\n    sampler = sampler,\n    n_samples = 3000,\n    n_chains = 4,\n)\n\nThe first positional value is the number of adaptation, or warmup, iterations. The second is the target acceptance probability delta. init_ϵ = 0.0 asks Turing to find an initial step size automatically.","title":"The Main NUTS Controls"},{"category":"section","location":"sampling/#Target-Acceptance-And-Step-Size","page":"Sampling","text":"At the end of an HMC trajectory, the proposal is accepted with probability\n\nalpha = minleft(1 exp(-Delta H)right)\n\nAn exact trajectory would conserve H and have alpha=1. Leapfrog integration introduces an energy error Delta H, so the acceptance rate is a direct signal of trajectory accuracy. During adaptation, NUTS changes its step size to approach the target δ.\n\nA lower target, such as 0.65, normally permits a larger step size and fewer gradient evaluations, but it can produce more rejected or divergent moves.\nA higher target, such as 0.8 or 0.9, normally produces a smaller, more accurate step size. It is often safer for difficult ODE posteriors but costs more per effective sample.\nAn extremely high target is not automatically better. Tiny steps can make every trajectory expensive while hiding a poor parameterization.\n\nThe mean acceptance_rate after warmup should be reasonably close to the target. Persistently low acceptance, numerical_error transitions, or large energy errors suggest that the step size is too large, the equations are not smooth, or some proposed ODE solves are unstable. Raise δ before manually forcing a small init_ϵ; manual init_ϵ is mainly useful when automatic initialization repeatedly starts at an unsafe scale.","title":"Target Acceptance And Step Size"},{"category":"section","location":"sampling/#Adaptation-Length","page":"Sampling","text":"Warmup learns the step size and metric. More adaptation is useful when there are many uncertain parameters, their scales differ greatly, a dense metric is used, or chains start far from typical posterior values. Too little adaptation leaves production sampling with poor geometry. More adaptation does not repair an invalid model and does not add posterior draws: it is an up-front cost.\n\nCompare adapted step sizes and metrics across chains. If they vary greatly, increase warmup and check whether the chains have reached the same posterior region. Reducing adaptation is sensible only after repeated runs show stable adapted settings from good initial points.","title":"Adaptation Length"},{"category":"section","location":"sampling/#Tree-Depth","page":"Sampling","text":"max_depth limits how many times NUTS may double its trajectory tree. A depth of d permits up to roughly 2^d leapfrog steps, so increasing depth by one can approximately double the worst-case work for a draw. A draw that often reaches the configured limit has been stopped by the limit rather than by the No-U-Turn criterion.\n\ntip: Intuition: a ball travelling through valleys\nTree depth limits how long NUTS lets the ball travel while it searches both directions along a valley. A shallow tree stops the ball before it has crossed a long valley, producing nearby, correlated samples. A deeper tree gives it time to travel around a curved or elongated valley until its path naturally starts to turn back. If the valley is extremely narrow or badly scaled, however, repeatedly extending the journey is wasteful: reshape the coordinates or metric instead of only allowing a longer trip.\n\nInspect both tree_depth and n_steps. Increase max_depth when many valid transitions hit the cap and effective sample size improves with longer trajectories. First improve the metric or parameterization when depth saturation accompanies tiny step sizes, numerical errors, or strong posterior correlations. Tree depth is a safety and cost limit, not a general cure.","title":"Tree Depth"},{"category":"section","location":"sampling/#Other-Controls","page":"Sampling","text":"Δ_max is the maximum allowed energy error before Turing treats a trajectory as divergent. The default is normally preferable: increasing it can hide bad trajectories instead of improving them. n_samples controls retained posterior draws, whereas the adaptation count controls warmup. More retained draws reduce Monte Carlo error only after the chain mixes; they do not rescue a stuck chain.\n\nUse multiple chains with dispersed, valid starting points. Agreement between chains is stronger evidence of convergence than one long chain. Random seeds are important for reproducibility but should not be tuned to obtain a preferred posterior.","title":"Other Controls"},{"category":"section","location":"sampling/#Metric-And-Parameter-Scale","page":"Sampling","text":"The metric determines how parameter displacement is measured and how momentum is assigned. If one uncertain parameter ranges around 10^-3 and another around 10^3, the same Euclidean step cannot suit both. The posterior then looks like a long, narrow valley: a step small enough not to cross its narrow side advances very slowly along its length.\n\nIdeally the sampling coordinates make plausible changes in every parameter roughly comparable. For example,\n\nz_i = fracq_i - mu_is_i\n\nuses a meaningful location mu_i and scale s_i. Positive parameters that span orders of magnitude are often better represented by z_i=log q_i; bounded parameters can use a logit transformation. Priors must be transformed consistently. Turing also transforms constrained distributions internally, but a scientifically appropriate model parameterization can still make the posterior much easier to sample.\n\nAdvancedHMC provides three useful Euclidean metrics:\n\nUnitEuclideanMetric uses an identity metric. It is cheap but assumes that every transformed parameter already has a similar scale and is nearly uncorrelated.\nDiagEuclideanMetric adapts one scale per parameter. It is the robust default and handles different parameter ranges, but not rotated or correlated valleys.\nDenseEuclideanMetric adapts a full matrix. It can rescale parameters and represent pairwise correlations, allowing much more direct movement through an angled valley. It needs more warmup and its estimation becomes expensive and noisy as dimension grows.\n\nStart with the diagonal metric. Try the dense metric when posterior plots show strong correlations, tree depth is high despite sensible marginal scales, and there are enough warmup samples to estimate a covariance matrix reliably. Use the unit metric only when parameters have already been standardized. Compare metrics by effective samples per second, numerical errors, depth saturation, and agreement of posterior summaries, not by runtime alone.","title":"Metric And Parameter Scale"},{"category":"section","location":"sampling/#Initial-Parameter-Values","page":"Sampling","text":"Initial values matter because every first trajectory needs a finite prior, likelihood, gradient, and successful ODE solution. A point near a constrained boundary or in an unstable simulation region can force a tiny step size or make warmup fail before NUTS reaches the typical set.\n\nFor run_inference, uncertain parameters start from SimulationSpec.uncertain_param_values when provided, otherwise from their scalar YAML value. Observation noise starts from TuringSpec.noise_initial. An initial value for a uniform prior must be strictly inside its bounds; a YAML value on the boundary falls back to the prior mean, while an invalid explicit override raises an error.\n\nChoose finite, scientifically plausible values inside the prior support and test one simulation at those values before sampling. The initial point need not be the posterior mode, but it should avoid pathological ODE regions. Use several dispersed starts to detect separate posterior modes rather than placing every chain at exactly the same point.\n\nAfter a pilot posterior is available, its draws can provide better starts for later runs. Transform and standardize the posterior draws, cluster them, and select a high-density representative or medoid from each cluster. These points can recover stable starts from distinct posterior regions without choosing an arbitrary draw. Preserve multiple clusters when they represent genuine modes; collapsing them to one start can conceal multimodality. Representatives can be placed in YAML or uncertain_param_values for later runs. Supplying a different representative to every chain currently requires separate run_inference calls or direct Turing initialization because the package-generated start is shared across chains.","title":"Initial Parameter Values"},{"category":"section","location":"sampling/#Equations-Must-Have-A-Smooth-Landscape","page":"Sampling","text":"NUTS can only use gradients that the model makes available. The common positivity guard\n\nx_+ = max(x0)\n\nis continuous but not differentiable at zero. Its derivative changes abruptly from zero to one. When an ODE trajectory crosses zero, this corner can make sensitivities discontinuous, damage leapfrog energy conservation, and lower acceptance. It can also create a flat negative region that gives NUTS no useful gradient back toward physically meaningful values.\n\nPrefer a model that enforces positivity by construction when scientifically appropriate. A smooth approximation to the positive part is\n\noperatornamesmoothpos_varepsilon(x)\n    = fracx + sqrtx^2 + varepsilon2\n\nwhere varepsilon controls the width of the smooth transition. A softplus or a positive-state parameterization is another option. The smoothing scale must be small relative to meaningful state values but not so small that it is numerically indistinguishable from the original corner. Any replacement changes the model near zero and must therefore be checked against simulations and posterior predictive behavior.\n\nImproving the posterior geometry in this way can make NUTS more efficient, not just more stable. Removing a nonsmooth construct such as max(0, x) gives the gradient a more continuous landscape, which can allow a larger step size, fewer numerical errors and divergences, and more effective samples per unit of computation. The replacement must still represent the intended scientific model.","title":"Equations Must Have A Smooth Landscape"},{"category":"section","location":"sampling/#Hill-Equations-In-The-Log-Domain","page":"Sampling","text":"For positive x, a Hill activation can be written as\n\nh(xKn) = fracx^nK^n+x^n\n         = frac11+expn(log K-log x)\n\nThe second expression is the same Hill model evaluated through a logistic function of log concentrations. A Hill repression is\n\nr(xKn) = frac11+(xK)^n\n         = frac11+expn(log x-log K)\n\nThe log-Hill form avoids directly raising very small or very large values to a power, reducing overflow, underflow, and unstable ratios. In code, use a carefully chosen positive floor or smooth-positive input before taking log. Compared with wrapping powers in max(x, 0), a smooth positive input followed by a log-Hill evaluation gives NUTS a smoother and usually more numerically stable landscape. It also makes the ratio xK explicit, which is useful when parameters span several orders of magnitude.\n\nThe log form improves numerical evaluation; it does not justify a different biological equation. Confirm that activation, repression, zero behavior, and units still match the intended mechanism.","title":"Hill Equations In The Log Domain"},{"category":"section","location":"sampling/#Diagnose-Before-Tuning","page":"Sampling","text":"The returned chain can contain NUTS diagnostics including acceptance_rate, tree_depth, n_steps, numerical_error, hamiltonian_energy, and step_size. Also inspect trace plots, rank or density plots, effective sample size (ESS), and widehat R across chains.\n\nSymptom Likely response\nLow acceptance or numerical errors Increase target δ; then inspect equation smoothness and ODE failures.\nFrequent maximum tree depth Improve posterior geometry, especially parameter scaling or the metric, first; increase max_depth if trajectories remain valid.\nHigh autocorrelation and low ESS Improve parameterization or metric; then consider longer trajectories or more draws.\nChains disagree or widehat R remains high Run longer warmup from dispersed starts and investigate multimodality.\nDense metric is unstable Add warmup, reduce dimension, regularize the model, or return to a diagonal metric.\nClean diagnostics but high Monte Carlo error Increase n_samples or the number of chains.\n\nA useful tuning order is:\n\nMake every initial simulation finite and place starts inside prior support.\nRemove discontinuities and numerically unstable equation forms.\nTransform badly ranged positive or bounded parameters.\nRun adequate warmup with a diagonal metric and several chains.\nTune target acceptance in the approximate range 0.65 to 0.9 using acceptance and numerical-error diagnostics.\nAddress repeated depth saturation by improving posterior geometry through better scaling, metric choice, or parameterization before raising max_depth.\nCompare diagonal and dense metrics by ESS per second and posterior agreement.\nIncrease retained samples only after the chains mix reliably.\n\nChange one group of settings at a time and compare posterior summaries, not only speed. The fastest chain is not useful if it explores the wrong region, and a high acceptance rate is not useful if each effective sample requires an excessively long trajectory.","title":"Diagnose Before Tuning"},{"category":"section","location":"reference/#Reference","page":"Reference","text":"The API below is ordered in the same chronology as a typical analysis script: load the YAML model, compile it with ModelingToolkit, wrap it in a runtime Model, define reusable simulation settings, run direct simulation or inference, and then scan design candidates.\n\nPages = [\"reference.md\"]","title":"Reference"},{"category":"section","location":"reference/#Load-A-YAML-Model","page":"Reference","text":"Start by loading the mechanistic model definition from YAML. The returned ModelDefinition is declarative: it contains symbolic equations, states, parameters, inputs, and metadata, but it is not yet an executable ODE problem.","title":"Load A YAML Model"},{"category":"section","location":"reference/#Compile-A-ModelingToolkit-System","page":"Reference","text":"Compile the equations from the loaded model definition with ModelingToolkit's System constructor. This is the bridge between the package's YAML layer and the SciML simulation layer.\n\nusing ModelingToolkit: @mtkcompile, System, t_nounits as t\n\nmodel_def = load_model(\"model.yml\")\n@mtkcompile sys = System(model_def.equations, t)\n\nSystem is provided by ModelingToolkit rather than UncertaintyOptimization.jl, so its full constructor API is documented upstream. Within this package workflow, pass the compiled sys directly to Model.","title":"Compile A ModelingToolkit System"},{"category":"section","location":"reference/#Create-The-Runtime-Model","page":"Reference","text":"Wrap the loaded definition and compiled system in Model. This object owns the cached ODE problem and parameter setters reused by simulation, inference, and scan calls.","title":"Create The Runtime Model"},{"category":"section","location":"reference/#Define-Simulation-Settings","page":"Reference","text":"Use SimulationSpec for observation times, observed state or states, initial conditions, time spans, solver choice, and solver options. The same spec is shared by direct simulation, TuringSpec, and CartesianScanner.","title":"Define Simulation Settings"},{"category":"section","location":"reference/#Simulate","page":"Reference","text":"simulate! is the direct simulation entry point. It also underpins inference and scan stages, which call it repeatedly after the model has been prepared.","title":"Simulate"},{"category":"section","location":"reference/#Infer-Uncertain-Parameters","page":"Reference","text":"Create a TuringSpec from a SimulationSpec and observed data, then pass it to run_inference. The resulting posterior samples can be passed to run_scan.","title":"Infer Uncertain Parameters"},{"category":"section","location":"reference/#Scan-Design-Candidates","page":"Reference","text":"Use CartesianScanner to define a Cartesian grid over design candidates and a loss function. run_scan evaluates that grid for each posterior sample. The same API is used for Thompson sampling and for later evaluation of selected candidates.","title":"Scan Design Candidates"},{"category":"section","location":"reference/#Lower-Level-Utilities","page":"Reference","text":"Most users do not need these directly, but they are exported or documented for advanced workflows that prepare models explicitly.","title":"Lower-Level Utilities"},{"category":"function","location":"reference/#UncertaintyOptimization.load_model","page":"Reference","text":"load_model(filename::String; input=nothing) -> ModelDefinition\n\nLoad a model definition from YAML.\n\nThis is the public entry point for the YAML stage. It reads the file with load_YAML, validates the required model sections, builds ModelingToolkit states and parameters, converts equation strings to symbolic equations, and returns a ModelDefinition.\n\nThe YAML file describes the model structure:\n\nexperiment: name and description.\nmodel: model type and state names.\nparameters: fixed, uncertain, and design parameter metadata.\nequations: right-hand sides for each state equation.\ninputs: an optional step input signal. A symbolic input may instead be supplied with the input keyword.\n\nThe returned definition is not yet an executable ODE problem. Compile it and wrap it in a Model, then use SimulationSpec, simulate!, TuringSpec, or CartesianScanner for later workflow stages.\n\n\n\n\n\n","title":"UncertaintyOptimization.load_model"},{"category":"type","location":"reference/#UncertaintyOptimization.ModelDefinition","page":"Reference","text":"ModelDefinition\n\nSymbolic model, states, parameters, and input expression parsed from YAML.\n\nA ModelDefinition is intentionally declarative. It stores the parsed model name and description, the model type, ModelingToolkit equations, symbolic states, parameter specifications, and the generated input signal. Compile it with ModelingToolkit and wrap it in Model before simulation, inference, or scan stages:\n\nmodel_def = load_model(\"model.yml\")\n@mtkcompile sys = System(model_def.equations, t)\nmodel = Model(model_def, sys)\n\n\n\n\n\n","title":"UncertaintyOptimization.ModelDefinition"},{"category":"type","location":"reference/#UncertaintyOptimization.Model","page":"Reference","text":"Model(model_def, sys)\n\nRuntime wrapper around a ModelDefinition and compiled ModelingToolkit system.\n\nModelDefinition stores the declarative YAML-derived model. Model adds the mutable runtime state needed by the later stages: the cached ODEProblem, warmup and production parameter setters, uncertain-parameter priors and initial values, and design-stage setter data.\n\nCreate a Model after compiling the equations:\n\nmodel_def = load_model(\"model.yml\")\n@mtkcompile sys = System(model_def.equations, t)\nmodel = Model(model_def, sys)\n\nThe same Model can then be used for simulation, Turing inference, Thompson sampling scans, and evaluation scans.\n\n\n\n\n\n","title":"UncertaintyOptimization.Model"},{"category":"type","location":"reference/#UncertaintyOptimization.SimulationSpec","page":"Reference","text":"SimulationSpec(; t_obs, obs_state, initial_conditions, tspan,\n               uncertain_param_values=Dict(), solver=Euler(),\n               solver_opts=NamedTuple())\n\nShared settings for the simulation/build stage.\n\nSimulationSpec captures the parts of a run that are not part of the YAML model definition: observation times, observed state or states, initial conditions, time spans, solver, and solver keyword options. The same spec can be reused by TuringSpec for inference and by ThompsonGridSpec/CartesianScanner for Thompson-style scans and later evaluation.\n\nFields:\n\nt_obs: time points saved for observations and loss evaluation.\nobs_state: one state name or a collection of state names to observe.\ninitial_conditions: initial values in the compiled system's unknown order.\ntspan: integration intervals. A single (start, stop) is normalized to ((start, stop), (start, stop)). Use ((warmup_start, warmup_stop), (production_start, production_stop)) when warmup and production solves should use different intervals.\nuncertain_param_values: optional values for uncertain parameters.\nsolver: OrdinaryDiffEq-compatible solver object.\nsolver_opts: keyword options passed through to solve.\n\n\n\n\n\n","title":"UncertaintyOptimization.SimulationSpec"},{"category":"function","location":"reference/#UncertaintyOptimization.simulate!","page":"Reference","text":"simulate!(model, simulation; kwargs...)\nsimulate!(model, initial_conditions, tspan; kwargs...)\nsimulate!(model, initial_conditions, parameters, tspan; kwargs...)\n\nRun the simulation/build stage for a model.\n\nsimulate! builds and caches the underlying ODEProblem on the first call by delegating to setup_simulation!. Later calls reuse that problem and the precomputed parameter setters, which is why inference and scan stages can call it repeatedly inside tight loops.\n\nWhen passed a SimulationSpec, simulate! uses the spec's initial conditions, time span, solver, observation times, observed states, solver options, and uncertain parameter values. Extra keyword arguments override values from the spec and are forwarded to the lower-level method.\n\ntspan can be either a single (start, stop) interval or a pair of intervals: ((warmup_start, warmup_stop), (production_start, production_stop)). A single interval is used for both warmup and production. A pair lets the warmup solve run longer than the production solves.\n\nThe simulation uses YAML/default parameter values plus any supplied overrides:\n\nparameters: scalar parameter overrides used when the problem is first built.\nsolver and solver_opts: solver object and keyword options for solve.\nsaveat and save_idxs: output times and observed state indices.\nsampled_uncertain_params: posterior draw values in model tunable order.\nparameter_setter and parameter_values: low-level setter used by grid scans.\ndesign: use design-stage warmup and multiparameter values.\nreturn_simulate: return (warmup_sol, sols) instead of only production solutions.\n\nThe return value is a vector of production solutions. If staged parameters are present, the vector contains one solution per stage. With return_simulate=true, the returned named tuple also includes the warmup solution, or nothing if no warmup values were configured.\n\n\n\n\n\n","title":"UncertaintyOptimization.simulate!"},{"category":"type","location":"reference/#UncertaintyOptimization.TuringSpec","page":"Reference","text":"TuringSpec(; simulation, data, noise_prior=InverseGamma(2, 3),\n           noise_initial=3.0, sampler=NUTS(0.65), n_samples=3000,\n           n_chains=1, sampling_method=MCMCSerial())\n\nSettings for Bayesian inference with Turing.\n\nThis spec represents the inference stage of the workflow. simulation defines how the model is solved for each proposed parameter draw; data contains the observations to compare against those solves; and the remaining fields configure the observation noise model and Turing sampler.\n\ndata may be a vector or matrix, but it is flattened internally. Its length must match the saved simulation layout:\n\nlength(t_obs) * observed_state_count(simulation) * number_of_production_solves\n\nUncertain parameters are discovered from YAML parameters with role :uncertain. Their priors are read from the YAML prior metadata, while noise_prior controls the observation noise sigma.\n\n\n\n\n\n","title":"UncertaintyOptimization.TuringSpec"},{"category":"function","location":"reference/#UncertaintyOptimization.run_inference","page":"Reference","text":"run_inference(model, spec)\n\nRun an inference stage using the implementation selected by spec.\n\nmodel should wrap a compiled ModelingToolkit system, and spec should be a concrete InferenceSpec such as TuringSpec. The concrete method prepares the model for simulation, repeatedly calls simulate! under the sampler, and returns posterior samples in the format produced by that backend.\n\nThis fallback method exists to make unsupported inference specifications fail with a clear error.\n\n\n\n\n\nrun_inference(model, spec::TuringSpec)\n\nRun Bayesian inference with Turing.\n\nThis method implements the inference stage for TuringSpec. It prepares the model for the shared SimulationSpec, builds priors for YAML parameters marked :uncertain, constructs the Turing model, samples with the configured sampler, and renames sampled chain columns from Turing's internal array names back to the model parameter symbols.\n\nThe returned chain can be passed directly, or after thinning/subsampling, to run_scan for Thompson sampling and evaluation.\n\n\n\n\n\n","title":"UncertaintyOptimization.run_inference"},{"category":"type","location":"reference/#UncertaintyOptimization.CartesianScanner","page":"Reference","text":"CartesianScanner\n\nAlias for ThompsonGridSpec.\n\nUse this name when the important detail is the Cartesian grid search over design candidates. Use ThompsonGridSpec when emphasizing the Thompson sampling stage that evaluates the grid under posterior draws.\n\n\n\n\n\n","title":"UncertaintyOptimization.CartesianScanner"},{"category":"type","location":"reference/#UncertaintyOptimization.ThompsonGridSpec","page":"Reference","text":"ThompsonGridSpec(; simulation, scan, loss)\n\nGrid-scan specification using a shared SimulationSpec.\n\nThompsonGridSpec defines the design candidates considered for each posterior sample. scan is a collection of named tuples with:\n\nsymbol: parameter to modify.\nvalues: candidate values or scale factors.\nkind: either :scale or :value.\n\nFor kind = :scale, each grid value multiplies the posterior draw when the symbol is uncertain, or the scalar YAML value when the symbol is fixed/design. For kind = :value, the grid value is used directly as the parameter value.\n\nloss receives the evaluated simulation output and returns a scalar. The default evaluator calls:\n\nloss(warmup_sol, predicted_sol; sys=model.sys)\n\nThe constructor validates scan axes and precomputes the Cartesian product in combinations.\n\n\n\n\n\n","title":"UncertaintyOptimization.ThompsonGridSpec"},{"category":"function","location":"reference/#UncertaintyOptimization.run_scan","page":"Reference","text":"run_scan(samples, spec::CartesianScanner, model::Model; evaluator = nothing)\n\nRun a grid scan for each sample in samples using the Cartesian product of spec.scan.\n\nThis is the shared engine for two workflow stages:\n\nThompson sampling: pass posterior samples and a candidate grid; select the is_best row for each posterior iteration.\nEvaluation: pass posterior samples and a reduced grid of candidate designs; aggregate the returned loss values across iterations.\n\nBy default, each candidate is evaluated using the settings from spec.simulation. The posterior sample is converted into the model's tunable parameter order, each scan axis is updated according to its kind, and the loss is evaluated as:\n\nspec.loss(sim.warmup_sol, predicted_sol; sys=model.sys)\n\nIf evaluator is provided, it is called first as:\n\nevaluated = evaluator(sample, grid_values, spec)\n\nThen the loss is evaluated as:\n\nspec.loss(evaluated...) if evaluated is a tuple\nspec.loss(evaluated) otherwise\n\nThis lets a caller provide an evaluator that turns (posterior_sample, grid_values, spec) into simulation outputs such as (warmup_sol, predicted_sol).\n\nReturns a DataFrame with one row per posterior iteration and scan candidate. Scan values are expanded into columns named from each scan axis, for example kx2_scaler for (symbol = :kx2, kind = :scale) and kx2_value for the resolved parameter value used by the simulation. For (symbol = :kx2, kind = :value), only kx2_value is emitted because the grid value is already the resolved parameter value. Each row also includes loss, is_best, and best_loss.\n\n\n\n\n\n","title":"UncertaintyOptimization.run_scan"},{"category":"function","location":"reference/#UncertaintyOptimization.setup_simulation!","page":"Reference","text":"setup_simulation!(model, initial_conditions, tspan; parameters=Dict())\n\nBuild and cache the ODE problem plus parameter setters used by simulate!.\n\nThis function is the explicit build step. It creates the ODEProblem, collects scalar parameter values from YAML and parameters, prepares warmup setters, detects staged production parameters, builds design-stage setters, and records the tunable uncertain parameters in the compiled system order expected by Turing.\n\nMost users can call simulate!, run_inference, or run_scan directly and let them call this function as needed. Call it yourself when you want to pay the build cost up front or inspect the prepared model fields before running a later stage.\n\n\n\n\n\n","title":"UncertaintyOptimization.setup_simulation!"},{"category":"section","location":"#UncertaintyOptimization","page":"Home","text":"Documentation for UncertaintyOptimization.\n\nPackage for risk-averse optimization under uncertainty.","title":"UncertaintyOptimization"},{"category":"section","location":"#Publication","page":"Home","text":"This package accompanies the workflow described in Risk-averse optimization of genetic circuits under uncertainty by Michal Kobiela, Diego A. Oyarzun, and Michael U. Gutmann, published in Cell Systems. The paper presents a design strategy for genetic circuits whose mechanistic models contain uncertain parameters and design parameters. Observed data from previous designs, including non-functional prototypes, are used to infer a posterior distribution over uncertain parameters. Candidate design parameters are then optimized through Thompson sampling and ranked with risk-averse summaries of predictive loss, so final designs are chosen for performance under epistemic uncertainty (posterior uncertainty) and any additional stochasticity you include in the model or loss.","title":"Publication"},{"category":"section","location":"#Install","page":"Home","text":"pkg> add https://github.com/MichalKobiela/UncertaintyOptimization.jl#1.0.0","title":"Install"},{"category":"section","location":"#ModelingToolkit-Integration","page":"Home","text":"The package uses ModelingToolkit.jl, or MTK, as the bridge between a model description and numerical simulation in Julia. YAML files describe states, equations, and parameter metadata. The package converts those declarations into symbolic ModelingToolkit equations, which are then compiled into a system and wrapped in a runtime Model.\n\nMTK provides several important advantages for this workflow:\n\nSymbolic model construction: equations can be manipulated and inspected before numerical simulation.\nConsistent parameter and state handling: symbolic names provide a stable interface for fixed, uncertain, and design parameters.\nSciML integration: compiled systems can be used to construct ODEProblems and solve them with the Julia differential-equation ecosystem.\nAnalytic Jacobians: MTK can derive Jacobians symbolically and provide them to numerical solvers, avoiding finite-difference approximations and often improving the efficiency and robustness of stiff ODE solves.\nReusable numerical setup: the compiled system, problem structure, and parameter setters can be prepared once and reused across many simulations.\nA path to model transformations: the same symbolic representation can support simplification, automatic equation transformations, sensitivity analysis, and extensions to larger mechanistic models.\n\nIn Julia, MTK is part of the SciML ecosystem: it handles the symbolic layer, while packages such as OrdinaryDiffEq provide numerical solvers and Turing provides probabilistic inference. UncertaintyOptimization.jl connects these layers. load_model parses the YAML model, Model holds the compiled MTK system and reusable simulation state, simulate! solves the resulting model, and the inference and scan stages repeatedly evaluate it for posterior and design parameter values.\n\nThe package workflow uses these stages:\n\nModel the system: YAML model files and load_model define the mechanistic model, states, equations, fixed parameters, uncertain parameters, and design parameters.\nIdentify uncertain and design parameters: YAML parameter roles separate uncertain parameters to infer from controllable design parameters to optimize.\nInfer uncertain parameters: TuringSpec and run_inference use observed data to produce posterior samples for parameters marked uncertain.\nDefine the design goal: the loss function supplied to CartesianScanner encodes the desired circuit behavior, such as matching a set point, trajectory, amplitude, or frequency.\nOptimize designs via Thompson sampling: CartesianScanner and run_scan evaluate candidate design values for each posterior draw. Each best candidate for a posterior draw is a grid-based Thompson sample.\nManage risk and select final designs: the run_scan output records candidate values, resolved parameters, losses, and best-design markers so downstream summaries can rank designs by median loss, upper quantiles, or other risk-averse criteria.","title":"ModelingToolkit Integration"},{"category":"section","location":"#Reference-Code","page":"Home","text":"The repository also keeps reference code for comparing the package workflow against earlier scripts. For this code, the docs use a one-page starter style: a single script that shows the full setup, mechanistic model, observed data, inference call, and saved output in one place. The current starter is experiments/reference/reproduce_original_code.jl, which reproduces the adaptation-circuit inference workflow with newer Julia libraries and colocated reference data.\n\nThe package workflow is organized around a small set of stages:\n\nload a symbolic model from YAML;\nbuild and reuse simulations with SimulationSpec and simulate!;\ninfer uncertain parameters from observed data with TuringSpec and run_inference;\nobtain Thompson samples by scanning design candidates with CartesianScanner and run_scan;\nevaluate candidate designs and summarize predictive loss for risk-averse selection.\n\nSee YAML Model Files for model-file structure, Workflow for the stage-by-stage guide, and Reference for generated API documentation.","title":"Reference Code"},{"category":"section","location":"#Implementation","page":"Home","text":"The original code developed by Michal Kobiela was transformed and extended by Mateusz Bieniek and Emma Pead as part of the Data Intelligence Hub in the School of Informatics at the University of Edinburgh. This work produced a more structured and reusable Julia package that integrates ModelingToolkit (MTK) with the SciML ecosystem, separating symbolic model construction from simulation, Bayesian inference, design scanning, and risk evaluation.\n\nModel definitions are expressed in YAML. This gives parameters an explicit structure, including their values, roles (fixed, uncertain, or design), priors, bounds, and staged warmup or production values. Equations are also declared in YAML and converted into symbolic MTK equations before the numerical problem is constructed. This makes models easier to inspect, modify, validate, and reuse across experiments.\n\nThe codebase also includes a testing framework covering model loading, simulation, inference, scanning, staged parameters, and YAML validation. These tests, together with clearer interfaces, documented workflows, validation, and reusable simulation setup, improve the overall quality, maintainability, and reproducibility of the implementation.\n\nPerformance has been improved by reusing compiled MTK systems and ODE problems, precomputing parameter setters, using analytic Jacobians where available, and reducing repeated setup work in inference and design scans. Bayesian inference has also been tuned for performance, including configuration and optimisation of the NUTS sampler for the model and data, while retaining the flexibility to adjust sampling settings for different experiments.","title":"Implementation"}]
}
